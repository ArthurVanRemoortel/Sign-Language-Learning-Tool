version: '0.1'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000 --insecure
    restart: always
    volumes:
      - .:/code
    ports:
      - "8080:8080"
    env_file:
      - .env.dev

volumes:
  redis_data: